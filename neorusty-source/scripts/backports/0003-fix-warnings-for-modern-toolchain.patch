Backport: fix warnings that newer rustc toolchains deny (-D warnings).
Resolves mismatched_lifetime_syntaxes (explicit lifetimes), unnecessary parens,
and dead_code lint for BiomeCodec so the pinned engine builds warning-clean.
diff --git a/pumpkin-registry/src/biomes.rs b/pumpkin-registry/src/biomes.rs
index fae78a9a..3fbc89b8 100644
--- a/pumpkin-registry/src/biomes.rs
+++ b/pumpkin-registry/src/biomes.rs
@@ -2,6 +2,7 @@ use pumpkin_protocol::VarInt;
 use serde::Serialize;
 
 #[derive(Debug, Clone, Serialize)]
+#[allow(dead_code)]
 pub struct BiomeCodec {
     name: String,
     id: i32,
diff --git a/pumpkin-world/src/world_gen/noise/density/unary.rs b/pumpkin-world/src/world_gen/noise/density/unary.rs
index 143ab1a2..578e1b61 100644
--- a/pumpkin-world/src/world_gen/noise/density/unary.rs
+++ b/pumpkin-world/src/world_gen/noise/density/unary.rs
@@ -65,7 +65,7 @@ pub struct UnaryFunction<'a> {
 }
 
 impl<'a> UnaryFunction<'a> {
-    pub(crate) fn create(action: UnaryType, input: Arc<DensityFunction<'a>>) -> UnaryFunction {
+    pub(crate) fn create(action: UnaryType, input: Arc<DensityFunction<'a>>) -> UnaryFunction<'a> {
         let base_min = input.min();
         let new_min = Self::internal_apply(&action, base_min);
         let new_max = Self::internal_apply(&action, input.max());
diff --git a/pumpkin/src/commands/mod.rs b/pumpkin/src/commands/mod.rs
index 36fa2abe..75eafd8b 100644
--- a/pumpkin/src/commands/mod.rs
+++ b/pumpkin/src/commands/mod.rs
@@ -75,4 +75,4 @@ pub fn default_dispatcher<'a>() -> CommandDispatcher<'a> {
 }
 
 type RunFunctionType =
-    (dyn Fn(&mut CommandSender, &Server, &ConsumedArgs) -> Result<(), InvalidTreeError> + Sync);
+    dyn Fn(&mut CommandSender, &Server, &ConsumedArgs) -> Result<(), InvalidTreeError> + Sync;
diff --git a/pumpkin/src/commands/tree_builder.rs b/pumpkin/src/commands/tree_builder.rs
index 1ef1416b..dcd82085 100644
--- a/pumpkin/src/commands/tree_builder.rs
+++ b/pumpkin/src/commands/tree_builder.rs
@@ -124,7 +124,7 @@ impl<'a> NonLeafNodeBuilder<'a> {
 
 /// Matches a sting literal.
 #[expect(dead_code)] // todo: remove (so far no commands requiring this are implemented)
-pub const fn literal(string: &str) -> NonLeafNodeBuilder {
+pub const fn literal(string: &str) -> NonLeafNodeBuilder<'_> {
     NonLeafNodeBuilder {
         node_type: NodeType::Literal { string },
         child_nodes: Vec::new(),
@@ -150,7 +150,7 @@ pub fn argument<'a>(name: &'a str, consumer: ArgumentConsumer) -> NonLeafNodeBui
 
 /// ```predicate``` should return ```false``` if requirement for reaching following [Node]s is not
 /// met.
-pub fn require(predicate: &(dyn Fn(&CommandSender) -> bool + Sync)) -> NonLeafNodeBuilder {
+pub fn require(predicate: &(dyn Fn(&CommandSender) -> bool + Sync)) -> NonLeafNodeBuilder<'_> {
     NonLeafNodeBuilder {
         node_type: NodeType::Require { predicate },
         child_nodes: Vec::new(),
diff --git a/pumpkin/src/server/connection_cache.rs b/pumpkin/src/server/connection_cache.rs
index 49eeff04..49c4008f 100644
--- a/pumpkin/src/server/connection_cache.rs
+++ b/pumpkin/src/server/connection_cache.rs
@@ -57,7 +57,7 @@ impl CachedBranding {
             cached_server_brand,
         }
     }
-    pub fn get_branding(&self) -> CPluginMessage {
+    pub fn get_branding(&self) -> CPluginMessage<'_> {
         CPluginMessage::new("minecraft:brand", &self.cached_server_brand)
     }
     fn build_brand() -> Vec<u8> {
diff --git a/pumpkin/src/server/key_store.rs b/pumpkin/src/server/key_store.rs
index becedf55..6a26359a 100644
--- a/pumpkin/src/server/key_store.rs
+++ b/pumpkin/src/server/key_store.rs
@@ -44,7 +44,7 @@ impl KeyStore {
         server_id: &'a str,
         verification_token: &'a [u8; 4],
         should_authenticate: bool,
-    ) -> CEncryptionRequest<'_> {
+    ) -> CEncryptionRequest<'a> {
         CEncryptionRequest::new(
             server_id,
             &self.public_key_der,
diff --git a/pumpkin/src/server/mod.rs b/pumpkin/src/server/mod.rs
index 8d264a19..57cd965b 100644
--- a/pumpkin/src/server/mod.rs
+++ b/pumpkin/src/server/mod.rs
@@ -170,7 +170,7 @@ impl Server {
         &'a self,
         verification_token: &'a [u8; 4],
         should_authenticate: bool,
-    ) -> CEncryptionRequest<'_> {
+    ) -> CEncryptionRequest<'a> {
         self.key_store
             .encryption_request("", verification_token, should_authenticate)
     }
